sudo: required
dist: trusty

language: node_js
# Will use version from .nvmrc


# safelist
branches:
  only:
  - master
  - stable

install:
  - cd bpmn-validation && npm install


script:
  - npm run test